# uncleaved 第二轮：SMD 测试

本目录用于本地完整输出的规范化位置。线上迁移包只含部分可用于分析的资料，不将其描述为完整 MD 输出。

规范源文件为 `run_smd_test_uncleaved_v01.sh`、`smd_pull_test_uncleaved_v01.mdin` 和 `smd_test_uncleaved_v01.RST`；它们由本地第二轮对应文件逐字节复制并校验。
