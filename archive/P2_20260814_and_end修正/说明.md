# P2 存档说明 — `&end` 修正 + 脚本拆段

**日期**: 2026-08-14
**版本内容**(11 文件/体系, `cleaved/` 与 `uncleaved/` 各一):
- 7 个 mdin(align_min + eq1_nvt + eq2–5_npt + smd_pull),约束块后**已含 `&end`**
  (pmemd/pmemd.cuda 标准格式);
- `smd.RST`(R0 = **28.80** → RF = 55.0, rk2 = 7.2);
- `run_equil.sh`(对齐极小化 → EQ1–EQ4, 到 EQ4 停);
- `run_smd.sh`(**SMD 单段版**: 从 eq4_npt.rst7 接续, 一口气拉到 RF; 不含分片)。

**相对 P1 的变更**:
1. 所有卡外约束块补 `&end`(修复 pmemd.cuda 的 `rfree: Error decoding variable from: LISTIN=POUT`);
2. 平衡与 SMD 拆成两个脚本(`run_equil.sh` / `run_smd.sh`),符合"核验后再投生产"。

**校验**:
- `cleaved/run_smd.sh` SHA-256 `bbcdb1653a27efcff43aba23382483e3fbd37044f6c279e19ee2d37496a0969c`
- `cleaved/smd.RST` SHA-256 `5714b00ba8ec4d0b241d3f028b11dbd19701de7250d4c9df8c1d1124f3c85fe2`

> 本存档从构建期暂存目录重建(P2 原始交付亦已被 P3 分片版替代)。含 `analyze_smd.py`? **不含**——P2 阶段尚无分析脚本(analyze v1 属 P3)。
> 指纹全表见 [../../ITERATION_HISTORY.md](../../ITERATION_HISTORY.md)。
