# P1 存档说明 — 初始交付(sander 兼容版)

**日期**: 2026-08-13
**版本内容**: 首轮交付的 M3 协议——7 个 mdin(align_min + eq1–5 + smd_pull)+ smd.RST(R0=28.80)。
特征:
- 约束块(DISANG/LISTIN/LISTOUT)**无 `&end`**(当时为适配 sander 已知缺陷);
- SMD 单段直跑(run_smd.sh 一口气跑到结束)。

**状态**: **无独立文件存档**——P2 修 `&end` 时原位覆盖了这些文件。
本目录仅存此记录。

**恢复线索**:
- 内容概述见 [docs/M3_模拟协议规划_v01.md](../../docs/M3_模拟协议规划_v01.md);
- 仓库 `SMD_ITERATION_HISTORY.md` 亦记录此版;
- 与 P2 的差异 = 是否含 `&end`(P2 起已含, pmemd 标准)。

**教训(已固化)**: Amber mdin 卡外约束块必须以 `&end` 终止(pmemd/pmemd.cuda 标准);
sander 需移除 `&end` 是其特例, 不以此为准(项目通用约定, 见 CLAUDE.md「语法必须用标准写法」)。
