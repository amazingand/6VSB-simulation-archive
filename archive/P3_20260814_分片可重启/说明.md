# P3 存档说明 — 分片可重启 + DUMPFREQ 修复

**日期**: 2026-08-14
**版本内容**(三个 tarball):
- `cleaved.tgz` / `uncleaved.tgz`(各体系完整文件集: 7 mdin + smd.RST + run_equil.sh + run_smd.sh + **analyze_smd.py v1**);
- `M3_SMD分片可重启包.tar.gz`(**分片版核心交付**: `run_smd_cleaved.sh` / `run_smd_uncleaved.sh` + `smd_pull_cleaved.mdin` / `smd_pull_uncleaved.mdin`)。

**相对 P2 的变更**:
1. **SMD 分片可重启**: 每段 1 ns(=500000 步), 从 `smd_chunkNN.rst7` 接续; 断点续跑靠
   rst7 存在性跳过; 跑完 `cat smd_chunk*.out > smd.out` 合并;
2. **DUMPFREQ 生产 bug 修复**: 模板注入 `&wt type='DUMPFREQ', istep1=5000 /`(此前 DUMPAVE 恒为空文件, analyze 无数据);
3. 命名脚本/模板体系建立(`run_smd_{cleaved,uncleaved}.sh` + `smd_pull_{cleaved,uncleaved}.mdin`);
4. analyze_smd.py **v1**: 力-位移/功 4 联图, 功跨段累计(段界自动重置检测)。

**已知局限**(P4 已修, 此处保留历史): R0 硬编码 28.80; 跳过条件只看 rst7(会静默跳过
截断段, 如初筛 cleaved chunk00); 段合并按通配符(换参数后混入旧段)。

**校验**:
- `cleaved.tgz` SHA-256 `2d68fc708a32101691e7abddde3ca4c1e65554a7272c7186a9df379bab935363`
- `uncleaved.tgz` SHA-256 `d338a100947a70bdb3981e38a504fee4fa99bd9ddb8f0f1f2840ecc520318143`
- `M3_SMD分片可重启包.tar.gz` SHA-256 `3d5bdec39c6de7bfbbdeea2d6f1b1b74fd4a9e938b2743ecb949bee20ea677c3`

> 指纹全表见 [../../ITERATION_HISTORY.md](../../ITERATION_HISTORY.md)。
